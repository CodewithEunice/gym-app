import React from "react";

const Button = () => {
  return (
    <div>
      <button
        className="semi-bold leading-none px-10 py-3 rounded-full
             text-white hover:bg-primary-400 bg-primary-300 cursor-pointer"
      >
        Submit
      </button>
    </div>
  );
};

export default Button;
